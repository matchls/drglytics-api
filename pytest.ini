[pytest]
# Met la racine du repo sur le sys.path pour que les tests dans tests/
# puissent faire `import stats_builder` (qui lui-même importe guid_mapper).
pythonpath = .
testpaths = tests
python_files = test_*.py
python_functions = test_*
